apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agent.fullname" . }}
  labels:
    {{- include "agent.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "agent.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "agent.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if or .Values.webhook.enabled .Values.webhook.webhookOnly }}
          command: ["./operator-controller"]
          args:
            {{- if .Values.env.agentId }}
            - --agent-id={{ .Values.env.agentId }}
            {{- end }}
            {{- if .Values.webhook.enabled }}
            - --enable-webhooks
            {{- end }}
            {{- if .Values.webhook.webhookOnly }}
            - --webhook-only
            {{- end }}
            {{- if .Values.webhook.certPath }}
            - --webhook-cert-path={{ .Values.webhook.certPath }}
            {{- end }}
            {{- if .Values.webhook.certName }}
            - --webhook-cert-name={{ .Values.webhook.certName }}
            {{- end }}
            {{- if .Values.webhook.certKey }}
            - --webhook-cert-key={{ .Values.webhook.certKey }}
            {{- end }}
            {{- if .Values.webhook.port }}
            - --webhook-port={{ .Values.webhook.port }}
            {{- end }}
            {{- if .Values.webhook.healthProbeAddr }}
            - --health-probe-bind-address={{ .Values.webhook.healthProbeAddr }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
            {{- if or .Values.webhook.enabled .Values.webhook.webhookOnly }}
            - name: webhook
              containerPort: {{ .Values.webhook.port | default 9443 }}
              protocol: TCP
            {{- end }}
          env:
            - name: AGENT_TOKEN
              value: {{ required "env.agentToken is required when installing the agent chart" .Values.env.agentToken | quote }}
            # Self-identity so the agent can restart/update its own Deployment in-place
            # (docs/agent-rollout). Deployment name is the chart fullname; namespace is
            # the release namespace (or the downward-API pod namespace as a fallback).
            - name: ANYPORT_AGENT_DEPLOYMENT
              value: {{ include "agent.fullname" . | quote }}
            - name: ANYPORT_AGENT_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            {{- /*
              The agent turns this into a Go soft memory limit (GOMEMLIMIT, applied at ~90% in
              applyMemoryLimit) so the GC works harder as the heap approaches the ceiling rather
              than ballooning through it on the startup informer sync and getting OOM-killed.

              Gated on the limit actually being declared on purpose: the downward API reports
              *node allocatable* for limits.memory when no limit is set, which would hand the
              runtime a ceiling the size of the machine and quietly undo the whole point. The
              nested `with` blocks are what make that gate safe — Go templates evaluate every
              argument to `and`, so a single combined condition would nil-panic the render for
              anyone who sets `resources: {}`.
            */}}
            {{- with .Values.resources }}
            {{- with .limits }}
            {{- if .memory }}
            - name: ANYPORT_MEMORY_LIMIT_BYTES
              valueFrom:
                resourceFieldRef:
                  containerName: {{ $.Chart.Name }}
                  resource: limits.memory
                  divisor: "1"
            {{- end }}
            {{- end }}
            {{- end }}
            {{- if .Values.env.agentId }}
            - name: AGENT_ID
              value: {{ .Values.env.agentId | quote }}
            {{- end }}
            {{- if .Values.env.portServerUrl }}
            - name: PORT_SERVER_URL
              value: {{ .Values.env.portServerUrl | quote }}
            {{- end }}
            {{- if .Values.env.grpcUseTls }}
            - name: GRPC_USE_TLS
              value: "true"
            {{- end }}
            {{- if .Values.env.logLevel }}
            - name: LOG_LEVEL
              value: {{ .Values.env.logLevel | quote }}
            {{- end }}
            {{- if .Values.env.autoHttps }}
            # Agent creates a Let's Encrypt ClusterIssuer (reusing the already-required
            # cert-manager) so routes get automatic HTTPS with no manual TLS secrets.
            - name: ANYPORT_AUTO_HTTPS
              value: "true"
            - name: ANYPORT_ACME_EMAIL
              value: {{ required "env.acmeEmail is required when env.autoHttps is true" .Values.env.acmeEmail | quote }}
            {{- end }}
            {{- /*
              Outside the autoHttps block on purpose. The agent binary defaults ANYPORT_AUTO_HTTPS to
              true when the variable is absent, and the ACME contact email usually arrives on the
              Cluster CR rather than through this chart — so a cluster can be issuing certificates
              with env.autoHttps never set here. Gating the directory on that flag would silently
              drop it on exactly those installs, leaving them ordering against production while
              the values file says staging.
            */}}
            {{- if .Values.env.acmeServer }}
            - name: ANYPORT_ACME_SERVER
              value: {{ .Values.env.acmeServer | quote }}
            {{- end }}
            {{- if .Values.env.gatewayEndpoint }}
            - name: GATEWAY_ENDPOINT
              value: {{ .Values.env.gatewayEndpoint | quote }}
            {{- end }}
            {{- if .Values.env.gatewayTLSInsecureSkipVerify }}
            - name: GATEWAY_TLS_INSECURE_SKIP_VERIFY
              value: "true"
            {{- end }}
          livenessProbe:
            exec:
              command:
                - pgrep
                - -f
                - "./operator-controller"
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          # Readiness reflects real platform connectivity (/readyz returns 200 only after the
          # agent logs in). This gates rolling self-updates: a new pod that can't reach the
          # control plane never becomes Ready, so Kubernetes keeps the old pod serving instead
          # of completing a broken update (docs/agent-rollout PHASE-4). Liveness stays a process
          # check so a transient control-plane outage never restarts a healthy agent.
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if or .Values.webhook.enabled .Values.webhook.webhookOnly }}
          volumeMounts:
            - name: webhook-cert
              mountPath: /tmp/k8s-webhook-server/serving-certs
              readOnly: true
          {{- end }}
      {{- if or .Values.webhook.enabled .Values.webhook.webhookOnly }}
      volumes:
        - name: webhook-cert
          secret:
            secretName: {{ include "agent.fullname" . }}-webhook-cert
      {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
