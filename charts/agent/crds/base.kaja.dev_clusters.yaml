---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusters.base.kaja.dev
spec:
  group: base.kaja.dev
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.statusEmoji
      name: Status
      type: string
    - jsonPath: .status.health
      name: Health
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the clusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines the desired state of Cluster
            properties:
              clusterRef:
                description: ClusterRef references the cluster where this resource
                  is deployed
                type: string
              description:
                description: Description provides additional context about the resource
                type: string
              displayName:
                description: DisplayName is a human-readable name for the resource
                type: string
              https:
                description: HTTPS configures the cluster's automatic-HTTPS behaviour.
                properties:
                  acmeEmail:
                    description: ACMEEmail is the contact email for Let's Encrypt
                      account registration.
                    type: string
                  autoBootstrap:
                    description: AutoBootstrap allows the agent to install ingress-nginx
                      + cert-manager without UI confirmation.
                    type: boolean
                  disabled:
                    description: |-
                      Disabled fully disables automatic HTTPS reconciliation.
                      No omitempty on json so that "disabled: false" is always serialised; omitting
                      false would leave the old true value in place when copySpec replaces the spec.
                    type: boolean
                  dns01:
                    description: |-
                      DNS01 configures ACME DNS-01 challenge solving, which is what makes wildcard certificates
                      possible. Nil means HTTP-01 only — the behaviour before docs/dns/PHASE-5.
                    properties:
                      accounts:
                        description: |-
                          Accounts are the per-zone challenge credentials, one per zone this cluster may issue a
                          wildcard for. They become the accounts JSON in a Secret cert-manager reads.

                          Kept out of GraphQL entirely: a Cluster CR is readable through the API by anyone who can read
                          the cluster, and these are credentials. They travel console → agent and nowhere else.
                        items:
                          description: |-
                            ACMEDNSAccountSpec is one acme-dns credential, in the shape cert-manager's acmeDNS solver expects
                            to find in its account Secret.

                            The whole type is `graphql:"ignore"`d on the spec field above, because a Cluster CR is readable
                            through the API by anyone who can read the cluster. These are credentials — narrow ones, able to
                            publish a TXT record at exactly one name and nothing else, but credentials — and there is no
                            reason for them to travel anywhere except console → agent.
                          properties:
                            domain:
                              description: |-
                                Domain is the zone this account answers challenges for. It is the map key cert-manager looks
                                the account up by: its solver indexes accounts by the challenge's DNS name, which for
                                "*.acme.com" is "acme.com" — an ACME authorization identifier never contains the wildcard.
                              type: string
                            fullDomain:
                              type: string
                            password:
                              type: string
                            subdomain:
                              type: string
                            username:
                              type: string
                          required:
                          - domain
                          - fullDomain
                          - password
                          - subdomain
                          - username
                          type: object
                        type: array
                      apiUrl:
                        description: APIURL is the acme-dns-compatible endpoint cert-manager
                          posts challenge values to.
                        type: string
                      wildcardZones:
                        description: |-
                          WildcardZones are the zones a wildcard certificate should be issued for. Also the solver's
                          dnsZones selector, so DNS-01 is used *only* for these names and every other host keeps
                          taking the HTTP-01 path it takes today.
                        items:
                          type: string
                        type: array
                    type: object
                  preferredIssuer:
                    description: PreferredIssuer pins a specific ClusterIssuer name
                      (overrides auto-pick).
                    type: string
                type: object
              networkIsolation:
                description: "NetworkIsolation controls whether the agent applies
                  the per-project NetworkPolicy\nbaseline (docs/network-isolation).\n\n\toff
                  \ — no policies are applied, and any previously applied baseline
                  is removed.\n\tauto — apply the baseline to every project namespace
                  on this cluster.\n\nDefaults to off. It stays opt-in until cross-project
                  plugin bindings are wired through\n(plan Phase 3): enforcing before
                  then would silently cut an app off from a database it\nis bound
                  to in another project."
                enum:
                - "off"
                - auto
                type: string
              projectRef:
                description: ProjectRef references the project this resource belongs
                  to
                type: string
              registryHost:
                description: |-
                  RegistryHost is the hostname the in-cluster registry serves, so the agent can mirror the
                  wildcard certificate covering it into the registry's namespace.

                  Carried from the console rather than derived here because the agent cannot work it out: the
                  hostname is built from console-side state (the cluster's tunnel hostname or verified public
                  domain), and a cluster holding a wildcard for its own zone *and* one for a customer's custom
                  domain gives the agent two candidates with nothing to choose between them. The console has
                  both the hostname and the certificate list, so the choice is made where the information is.

                  Empty when no registry is installed.
                type: string
            type: object
          status:
            description: ClusterStatus defines the observed state of Cluster
            properties:
              checksCompleted:
                type: string
              conditions:
                description: Conditions represent the current state of the Cluster
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              events:
                description: Events holds recent Warning events for the resource's
                  workload, collected best-effort. Optional.
                items:
                  description: EventInfo holds recent Warning events for the resource's
                    workload (best-effort).
                  properties:
                    count:
                      format: int32
                      type: integer
                    lastSeen:
                      type: string
                    message:
                      type: string
                    object:
                      type: string
                    reason:
                      type: string
                  required:
                  - reason
                  type: object
                type: array
              https:
                description: HTTPS provides the observed state of automatic HTTPS
                  in the cluster.
                properties:
                  activeIssuer:
                    description: |-
                      ActiveIssuer is the ClusterIssuer name routes should use. Empty if none yet.
                      Stage (a) leaves this empty — the picker in stage (b) will set it.
                    type: string
                  candidates:
                    description: Candidates is the list of discovered ClusterIssuers
                      in the cluster.
                    items:
                      description: IssuerCandidate describes a ClusterIssuer discovered
                        in the cluster.
                      properties:
                        email:
                          description: Email is the ACME account contact email.
                          type: string
                        ingressClass:
                          description: IngressClass declared on the issuer's http01
                            solver.
                          type: string
                        isAcme:
                          description: IsACME indicates the issuer is ACME-based (false
                            → likely private CA, ignore for public hosts).
                          type: boolean
                        name:
                          description: Name of the ClusterIssuer.
                          type: string
                        ready:
                          description: Ready reflects the Ready condition on the ClusterIssuer.
                          type: boolean
                        server:
                          description: Server is the ACME server URL (empty for non-ACME
                            issuers).
                          type: string
                        solverType:
                          description: 'SolverType: "http01" | "dns01" | "".'
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  certManager:
                    description: CertManager observed state.
                    properties:
                      installed:
                        description: Installed indicates whether cert-manager is present
                          in the cluster.
                        type: boolean
                      namespace:
                        description: Namespace where cert-manager is installed.
                        type: string
                      version:
                        description: Version of cert-manager, if detected.
                        type: string
                    type: object
                  ingressClass:
                    description: IngressClass resolved from the detected controller
                      (e.g. "nginx", "traefik").
                    type: string
                  pendingDecision:
                    description: |-
                      PendingDecision is populated only when human input is required.
                      Stage (a) leaves this nil.
                    properties:
                      options:
                        description: Options the user can choose from (issuer names,
                          install actions, etc.).
                        items:
                          type: string
                        type: array
                      plan:
                        description: Plan is a human-readable preview of what the
                          agent will do once the decision is approved.
                        type: string
                      question:
                        description: Question identifier — e.g. "missing_acme_email"
                          | "ambiguous_issuer" | "no_ingress_controller".
                        type: string
                    required:
                    - question
                    type: object
                  phase:
                    description: 'Phase: Discovering | Ready | NeedsApproval | Installing
                      | Failed'
                    type: string
                  reason:
                    description: 'Reason: Reused | Created | Provided | None'
                    type: string
                  wildcardCerts:
                    description: |-
                      WildcardCerts reports the shared wildcard certificates this cluster holds, and is how the
                      route controller learns one exists. Routes and certificates are reconciled by different
                      controllers, so the certificate's location and readiness have to travel between them
                      somehow; the Cluster status is the channel the route controller already reads for its
                      issuer and ingress class.
                    items:
                      description: WildcardCertStatus locates one wildcard certificate
                        and says whether it can be used yet.
                      properties:
                        namespace:
                          type: string
                        ready:
                          description: |-
                            Ready is true only once the secret actually exists. Attaching a not-yet-issued secret to an
                            Ingress does not fail — the ingress controller quietly falls back to its own default
                            certificate, and every visitor gets a name-mismatch warning — so a route must wait for this
                            rather than optimistically referencing the name.
                          type: boolean
                        secretName:
                          description: SecretName and Namespace locate the TLS secret
                            cert-manager wrote.
                          type: string
                        zone:
                          description: Zone the certificate covers, e.g. "acme.com"
                            for "*.acme.com" + "acme.com".
                          type: string
                      required:
                      - namespace
                      - secretName
                      - zone
                      type: object
                    type: array
                type: object
              message:
                type: string
              network:
                description: Network provides a summary of the cluster networking
                properties:
                  detectedCni:
                    description: |-
                      DetectedCNI is the container network plugin found on the cluster (e.g. "calico",
                      "cilium", "flannel"). Empty when undetectable.
                    type: string
                  detectedIngressController:
                    description: |-
                      DetectedIngressController is the name of the ingress controller found (e.g. "traefik", "ingress-nginx").
                      Empty if none detected; used to show which controller is in use.
                    type: string
                  detectedIngressControllerVersion:
                    description: DetectedIngressControllerVersion is the version of
                      the detected ingress controller (from container image tag).
                    type: string
                  hasCertManager:
                    description: HasCertManager indicates if cert-manager is detected
                    type: boolean
                  hasIngressController:
                    description: HasIngressController indicates if an ingress controller
                      is detected
                    type: boolean
                  ingressControllerNamespaces:
                    description: |-
                      IngressControllerNamespaces lists every namespace an ingress controller was found in.
                      Project NetworkPolicies allow ingress from these so Routes keep working; a hardcoded
                      namespace would break Routes on clusters using traefik (the k3s default).
                    items:
                      type: string
                    type: array
                  loadBalancerIps:
                    description: LoadBalancerIPs is the list of public IPs or hostnames
                      for the cluster
                    items:
                      type: string
                    type: array
                  networkPolicyEnforced:
                    description: |-
                      NetworkPolicyEnforced reports whether the detected CNI actually enforces NetworkPolicy.
                      False means any policy we apply is a silent no-op, which must be surfaced honestly
                      rather than presented as isolation.
                    type: boolean
                  networkPolicyEvidence:
                    description: |-
                      NetworkPolicyEvidence is what the detection matched on, in words. It carries the
                      distinction a bool cannot: "we recognised flannel and it does not enforce" and "we did not
                      recognise this cluster's CNI at all" both arrive as false, and they call for different
                      actions from whoever reads them. "Your projects are not isolated" is also a claim a user is
                      entitled to check rather than take.
                    type: string
                  nodeInternalIps:
                    description: |-
                      NodeInternalIPs lists the internal IPs of cluster nodes. Allowed as ingress in project
                      NetworkPolicies so kubelet readiness/liveness probes survive a default-deny.
                    items:
                      type: string
                    type: array
                  podCidrs:
                    description: PodCIDRs is the list of CIDRs used for pods
                    items:
                      type: string
                    type: array
                  registryClusterIp:
                    description: |-
                      RegistryClusterIP is the ClusterIP of the in-cluster registry's own Service.

                      Reported so the registry's hostname can resolve to an address that is reachable from a node
                      but routable from nowhere else. A node reaches ClusterIPs because kube-proxy programs the
                      DNAT rules on the node itself — which is what lets containerd pull an image without the
                      registry being exposed to the internet at all.

                      The registry's Service, not the ingress controller's, and the difference is the point: an
                      Ingress rule is served by the shared ingress listener, which on a public cluster answers on a
                      public address for any Host header that matches. Pointing at the registry's own Service
                      leaves no listener outside the cluster that can reach it.

                      Empty until the registry is installed, which is why publication is level-triggered.
                    type: string
                  serviceCidrs:
                    description: ServiceCIDRs is the list of CIDRs used for services
                    items:
                      type: string
                    type: array
                  tunnelIngressTarget:
                    description: |-
                      TunnelIngressTarget is the cluster-internal URL for the tunnel to route traffic to (e.g. http://ingress-nginx-controller.ingress-nginx.svc.cluster.local:80).
                      Set by the cluster reconciler when an ingress controller is detected; used by the console to configure the Cloudflare tunnel.
                    type: string
                type: object
              nextRunAt:
                description: NextRunAt is when a scheduled resource fires next, RFC3339.
                  Optional.
                type: string
              nodes:
                description: Nodes provides a summary of the cluster nodes
                properties:
                  allocatable:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Allocatable represents the total resources allocatable
                      of all nodes
                    type: object
                  architectures:
                    description: Architectures tracks the architectures of the nodes
                    items:
                      type: string
                    type: array
                  capacity:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Capacity represents the total resources capacity
                      of all nodes
                    type: object
                  count:
                    description: Count is the total number of nodes
                    type: integer
                  items:
                    description: Items holds per-node details for UI display (like
                      PodInfo for env/containerapp)
                    items:
                      description: NodeInfo holds per-node details for display (e.g.
                        on Cluster overview).
                      properties:
                        architecture:
                          description: Architecture is the node architecture (e.g.
                            amd64, arm64)
                          type: string
                        name:
                          description: Name is the node name
                          type: string
                        ready:
                          description: Ready indicates if the node is Ready (from
                            NodeReady condition)
                          type: boolean
                        version:
                          description: Version is the Kubernetes version (kubelet)
                            reported by the node
                          type: string
                      required:
                      - name
                      - ready
                      type: object
                    type: array
                  osImages:
                    description: OSImages tracks the OS images of the nodes
                    items:
                      type: string
                    type: array
                  versions:
                    description: Versions tracks the Kubernetes versions of the nodes
                    items:
                      type: string
                    type: array
                required:
                - count
                type: object
              observedGeneration:
                description: ObservedGeneration is the console desired-state version
                  (kaja.dev/generation) that Phase/Reason describe.
                format: int64
                type: integer
              observedRunId:
                description: |-
                  ObservedRunID is the one-off run request the controller has already acted on. Without it a
                  resync re-applying the same spec would start the run again. Optional.
                type: string
              phase:
                description: Phase is the derived workload health (e.g., Running,
                  Crashing, ImageError).
                type: string
              pods:
                description: Pods holds basic pod details for display (e.g. ContainerApp
                  overview). Optional.
                items:
                  description: PodInfo holds basic pod details; Reason/Message surface
                    container waiting/terminated state.
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    ready:
                      type: string
                    reason:
                      type: string
                    restartCount:
                      format: int32
                      type: integer
                  required:
                  - name
                  - phase
                  - ready
                  - restartCount
                  type: object
                type: array
              reason:
                description: Reason is a plain-language explanation of Phase.
                type: string
              runs:
                description: Runs holds recent executions of a run-to-completion resource
                  (e.g. a Task). Optional.
                items:
                  description: |-
                    RunInfo is one execution of something that runs to completion — today a Task's run, and the
                    same shape a BuildRun would need. Timestamps are RFC3339 strings, matching EventInfo, so the
                    status stays plain JSON the console can render without a type in between.
                  properties:
                    finishedAt:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    reason:
                      type: string
                    startedAt:
                      type: string
                    trigger:
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
              services:
                description: Services holds service details for the resource (e.g.
                  HelmApp release services) for domain/route backend selection. Optional.
                items:
                  description: ServiceInfo holds service details for display and domain
                    backend selection.
                  properties:
                    name:
                      type: string
                    ports:
                      items:
                        description: ServicePortInfo holds a single port exposed by
                          a service (for domain/route backend selection).
                        properties:
                          name:
                            type: string
                          port:
                            format: int32
                            type: integer
                          protocol:
                            type: string
                        required:
                        - port
                        type: object
                      type: array
                  required:
                  - name
                  type: object
                type: array
              status:
                type: string
              statusEmoji:
                type: string
              steps:
                items:
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    status:
                      type: string
                  required:
                  - message
                  - name
                  - status
                  type: object
                type: array
              tools:
                description: Tools observes the status of various tools in the cluster
                items:
                  description: ToolStatus defines the observed state of a tool
                  properties:
                    installed:
                      description: Installed indicates if the tool is currently installed/detected
                      type: boolean
                    message:
                      description: Message provides additional information about the
                        tool status
                      type: string
                    name:
                      description: Name of the tool
                      type: string
                    version:
                      description: Version of the installed tool (if detectable)
                      type: string
                  required:
                  - installed
                  - name
                  type: object
                type: array
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
