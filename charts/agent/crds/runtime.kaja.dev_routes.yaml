---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: routes.runtime.kaja.dev
spec:
  group: runtime.kaja.dev
  names:
    kind: Route
    listKind: RouteList
    plural: routes
    singular: route
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Route is the Schema for the routes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          httpsStatus:
            description: |-
              httpsStatus holds the observed HTTPS/TLS certificate state for this route.
              Updated by the route controller independently of the main status checks.
            properties:
              message:
                description: Message provides a human-readable description of the
                  current phase
                type: string
              phase:
                description: 'Phase is the current HTTPS certificate phase: HTTP |
                  Issuing | Ready | Failed'
                type: string
            required:
            - phase
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Route
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations specifies additional annotations to add to
                  the Ingress
                type: object
              backend:
                description: Backend specifies the backend service for the route
                properties:
                  serviceName:
                    description: ServiceName specifies the name of the backend service
                    type: string
                  servicePort:
                    description: |-
                      ServicePort specifies the numeric port of the backend service.
                      Ignored when ServicePortName is provided.
                    format: int32
                    type: integer
                  servicePortName:
                    description: |-
                      ServicePortName specifies the named port of the backend service (e.g. "http").
                      When set this takes precedence over ServicePort, making the Ingress resilient
                      to port-number changes in the ContainerApp.
                    type: string
                required:
                - serviceName
                type: object
              backendNamespace:
                description: BackendNamespace is the namespace where the backend Service
                  lives. When set, the Ingress is created in this namespace so it
                  can reference the Service (e.g. for HelmApp routes where the release
                  is in a different namespace than the Route).
                type: string
              clusterRef:
                description: ClusterRef references the cluster where this resource
                  is deployed
                type: string
              description:
                description: Description provides additional context about the resource
                type: string
              displayName:
                description: DisplayName is a human-readable name for the resource
                type: string
              host:
                description: Host specifies the domain name for the route
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels specifies additional labels to add to the Ingress
                type: object
              path:
                description: Path specifies the URL path for the route
                type: string
              projectRef:
                description: ProjectRef references the project this resource belongs
                  to
                type: string
              rules:
                description: Rules specifies the routing rules for the route
                items:
                  description: RouteRule specifies a routing rule for a route
                  properties:
                    backend:
                      description: Backend specifies the backend service for this
                        rule
                      properties:
                        serviceName:
                          description: ServiceName specifies the name of the backend
                            service
                          type: string
                        servicePort:
                          description: |-
                            ServicePort specifies the numeric port of the backend service.
                            Ignored when ServicePortName is provided.
                          format: int32
                          type: integer
                        servicePortName:
                          description: |-
                            ServicePortName specifies the named port of the backend service (e.g. "http").
                            When set this takes precedence over ServicePort, making the Ingress resilient
                            to port-number changes in the ContainerApp.
                          type: string
                      required:
                      - serviceName
                      type: object
                    host:
                      description: Host specifies the domain name for this rule
                      type: string
                    path:
                      description: Path specifies the URL path for this rule
                      type: string
                  type: object
                type: array
              setupLock:
                description: |-
                  SetupLock gates the route behind HTTP basic auth. Present means locked.
                  No omitempty on json, same as Cluster's HTTPS.Disabled: copySpec replaces the spec, so
                  an omitted field keeps whatever the CR already had and unlocking could never clear it.
                properties:
                  realm:
                    description: Realm is shown in the browser's credential prompt.
                    type: string
                  secretName:
                    description: |-
                      SecretName names a Secret in the Ingress namespace holding one htpasswd line under the
                      single key "auth". Exactly one key: traefik refuses a basic-auth secret with more than
                      one element, and nginx looks this one up by name.
                    type: string
                required:
                - secretName
                type: object
              tls:
                description: TLS specifies the TLS configuration for the route
                properties:
                  secretName:
                    description: SecretName specifies the name of the TLS secret containing
                      the certificate
                    type: string
                required:
                - secretName
                type: object
            required:
            - backend
            - host
            type: object
          status:
            description: status defines the observed state of Route
            properties:
              checksCompleted:
                type: string
              events:
                description: Events holds recent Warning events for the resource's
                  workload, collected best-effort. Optional.
                items:
                  description: EventInfo holds recent Warning events for the resource's
                    workload (best-effort).
                  properties:
                    count:
                      format: int32
                      type: integer
                    lastSeen:
                      type: string
                    message:
                      type: string
                    object:
                      type: string
                    reason:
                      type: string
                  required:
                  - reason
                  type: object
                type: array
              message:
                type: string
              nextRunAt:
                description: NextRunAt is when a scheduled resource fires next, RFC3339.
                  Optional.
                type: string
              observedGeneration:
                description: ObservedGeneration is the console desired-state version
                  (kaja.dev/generation) that Phase/Reason describe.
                format: int64
                type: integer
              observedRunId:
                description: |-
                  ObservedRunID is the one-off run request the controller has already acted on. Without it a
                  resync re-applying the same spec would start the run again. Optional.
                type: string
              phase:
                description: Phase is the derived workload health (e.g., Running,
                  Crashing, ImageError).
                type: string
              pods:
                description: Pods holds basic pod details for display (e.g. ContainerApp
                  overview). Optional.
                items:
                  description: PodInfo holds basic pod details; Reason/Message surface
                    container waiting/terminated state.
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    ready:
                      type: string
                    reason:
                      type: string
                    restartCount:
                      format: int32
                      type: integer
                  required:
                  - name
                  - phase
                  - ready
                  - restartCount
                  type: object
                type: array
              reason:
                description: Reason is a plain-language explanation of Phase.
                type: string
              runs:
                description: Runs holds recent executions of a run-to-completion resource
                  (e.g. a Task). Optional.
                items:
                  description: |-
                    RunInfo is one execution of something that runs to completion — today a Task's run, and the
                    same shape a BuildRun would need. Timestamps are RFC3339 strings, matching EventInfo, so the
                    status stays plain JSON the console can render without a type in between.
                  properties:
                    finishedAt:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    reason:
                      type: string
                    startedAt:
                      type: string
                    trigger:
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
              services:
                description: Services holds service details for the resource (e.g.
                  HelmApp release services) for domain/route backend selection. Optional.
                items:
                  description: ServiceInfo holds service details for display and domain
                    backend selection.
                  properties:
                    name:
                      type: string
                    ports:
                      items:
                        description: ServicePortInfo holds a single port exposed by
                          a service (for domain/route backend selection).
                        properties:
                          name:
                            type: string
                          port:
                            format: int32
                            type: integer
                          protocol:
                            type: string
                        required:
                        - port
                        type: object
                      type: array
                  required:
                  - name
                  type: object
                type: array
              status:
                type: string
              statusEmoji:
                type: string
              steps:
                items:
                  properties:
                    message:
                      type: string
                    name:
                      type: string
                    status:
                      type: string
                  required:
                  - message
                  - name
                  - status
                  type: object
                type: array
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
